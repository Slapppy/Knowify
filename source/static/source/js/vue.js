const form = document.querySelector('form');

form.addEventListener('submit', (e) => {
  e.preventDefault();
  // Perform form validation and submission
});

// Add animation class to the form to trigger the animation
form.classList.add('form-animation');

